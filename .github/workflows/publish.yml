name: Publish mod

on:
  push:
    branches:
      - 'versions/*.*'
  workflow_dispatch:

env:
  JAVA_VERSION: 21

permissions:
  contents: write
  
jobs:
  determine-versions:
    runs-on: ubuntu-latest
    outputs:
      minecraft_version: ${{ steps.minecraft.outputs.minecraft_version }}
      mod_version: ${{ steps.mod.outputs.mod_version }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: minecraft
        name: Get minecraft version
        run: |
          grep 'minecraft_version=.*' gradle.properties >> "$GITHUB_OUTPUT"
      - id: mod
        name: Get mod version
        run: |
          grep 'mod_version=.*' gradle.properties >> "$GITHUB_OUTPUT"
  build-and-publish:
    #if: contains(github.ref_name, 'versions/') # Is version branch
    runs-on: ubuntu-latest
    needs: determine-versions
    steps:
      - name: Check Environment Variables
        run: env

      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: true
          lfs: true
          fetch-depth: 1

      - name: Setup Java
        uses: actions/setup-java@v2
        with:
          distribution: "temurin"
          java-version: ${{ env.JAVA_VERSION }}

      - name: Make Gradle Wrapper Executable
        if: ${{ runner.os != 'Windows' }}
        run: chmod +x ./gradlew

      - name: Publish Locally
        run: |
          ./gradlew publishFabricPublicationToMavenLocal
          ./gradlew publishNeoforgePublicationToMavenLocal

      - name: Publish
        env:
          MINECRAFT_VERSION: ${{needs.determine-versions.outputs.minecraft_version}}
          MOD_VERSION: ${{needs.determine-versions.outputs.mod_version}}
          GITHUB_TOKEN: ${{ secrets.PUBLISH_GITHUB_TOKEN }}
        run: |
          mvn -s settings.maven.xml --batch-mode -f build/fabric/publications/fabric/pom-default.xml deploy
          mvn -s settings.maven.xml --batch-mode -f build/neoforge/publications/neoforge/pom-default.xml deploy
